import { makeStyles } from '@mui/styles';

export const useCommonStyles = makeStyles({
  itemTitle: {
    marginLeft: 10,
    fontSize: 14
  },
  sectionTitle: {
    fontWeight: 'bold',
    marginTop: 15,
  },
  sectionBody: {
    marginTop: 10,
  },
});
