import { makeStyles } from '@mui/styles';

export const useLandingSectionStyles = makeStyles({
  container: {
    position: 'relative',
    display: 'flex',
    flexDirection: 'column',
    minHeight: 400,
  },
});
